services:
  api1:
    image: "rinha-2026:simulation-known-ids"
    build:
      context: .
      dockerfile: Dockerfile.simulation
    environment:
      RINHA_SIMULATE_KNOWN_IDS: "1"

  api2:
    image: "rinha-2026:simulation-known-ids"
    build:
      context: .
      dockerfile: Dockerfile.simulation
    environment:
      RINHA_SIMULATE_KNOWN_IDS: "1"

  lb:
    image: "rinha-2026:simulation-known-ids"
    build:
      context: .
      dockerfile: Dockerfile.simulation
